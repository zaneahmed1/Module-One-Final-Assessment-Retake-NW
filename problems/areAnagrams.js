/** Returns whether two strings are anagrams.  Two strings are anagrams if they contain exactly the same letters in any order.
 * 
* @param {string} strOne - The first string
* @param {string} strTwo - The second string
* @returns {boolean} - True if the strings are anagrams, false otherwise
*/
function areAnagrams() {

}

module.exports = areAnagrams

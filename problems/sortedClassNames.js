/**
* Returns an array of class names sorted in ascending lexigraphical order
* 
* @param {object[]} arr - The input array. Objects will be in the form: { subject: "math", studentCount: 10 }
* @returns {string[]} - Returns an array of class names, sorted in ascending lexigraphical order
* 
*/

function sortedClassNames() {

}

module.exports = sortedClassNames
